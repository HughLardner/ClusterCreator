.idea
.terraform*
terraform/secrets.tf
terraform/terraform.tfstate*
ansible/tmp
*.DS_Store
scripts/.env
# Ansible Vault files (unencrypted backups)
ansible/**/vault/*.yaml.bak
ansible/**/vault/*.yml.bak
# Sealed Secrets private keys (if accidentally committed)
sealed-secrets-private-key.pem
sealed-secrets-public-key.pem
# Cursor IDE files
.cursor/
